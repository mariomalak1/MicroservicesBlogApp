info:
  name: create event
  type: http
  seq: 2

http:
  method: POST
  url: "{{events-service}}/"
  body:
    type: json
    data: |-
      {
        "type": "PostCreated",
        "data": {
          "title": "asimdsd",
          "content": "kasdmksadm"
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
